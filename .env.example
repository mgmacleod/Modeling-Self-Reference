# N-Link Analysis - Environment Configuration
#
# Copy this file to .env and customize as needed.
# .env is gitignored and will not be committed.

# ==============================================
# Docker Compose Port Configuration
# ==============================================
# Customize host ports if defaults conflict with other services

# API Server (default: 8000)
# NLINK_API_PORT=8000

# Basin Geometry Viewer (default: 8055)
# NLINK_BASIN_PORT=8055

# Multiplex Analyzer (default: 8056)
# NLINK_MULTIPLEX_PORT=8056

# Tunneling Explorer (default: 8060)
# NLINK_TUNNELING_PORT=8060

# ==============================================
# HuggingFace Configuration
# ==============================================
# Token for authenticated HF downloads (optional, enables higher rate limits)
# Get from https://huggingface.co/settings/tokens
# Requires write access for dataset uploads
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Custom cache directory for HF downloads (optional)
# HF_CACHE_DIR=/path/to/cache

# ==============================================
# Data Source Configuration
# ==============================================
# Data source: "local" or "huggingface" (default: local)
# DATA_SOURCE=local

# HuggingFace dataset repository (default: mgmacleod/wikidata1)
# HF_DATASET_REPO=mgmacleod/wikidata1
